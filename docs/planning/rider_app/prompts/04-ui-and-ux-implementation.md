# 04 — UI & UX implementation (rider app)

**Goal:** Polish consumer UI per `ui-design-system.md`; maps; loading/error states; ad playback UX.

## Deliverables

1. Map widgets for pickup/dropoff and live driver tracking.
2. Design system tokens (colors, typography, trip state banners).
3. **Taxi Assist Media** flow: modal/surface on trip start; rating+comment gate; deep link return handling; abandon = no credit (client + server rules).
4. Wallet and Media summary screens from `app-prd.md` §3.2.
5. Accessibility pass (contrast, tap targets).

## Acceptance

- UX review checklist in `docs/system/rider_app/` (create `ux-checklist.md`) ticked for MVP screens.

**Depends on:** prompt 03 core flows working.
