# 02 — Content & routes (Trip Website)

**Goal:** All core static pages wired with purposeful placeholder copy (replace via APD-Market later).

## Deliverables

1. Routes: `/`, `/about`, `/riders`, `/drivers`, `/advertise`, `/contact`, `/legal/privacy`, `/legal/terms` (stubs OK).
2. Internal linking matrix per `user-flows-and-ux-logic.md`.
3. SEO: `metadata` export per page (title/description).

## Acceptance

- All nav links work; no dead 404s in primary menu.
