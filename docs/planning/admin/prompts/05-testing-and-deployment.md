# 05 — Testing & deployment (admin)

**APD placeholder.**

- Add Playwright (or chosen E2E) smoke paths for login + one read-only page.
- Document env vars (`NEXT_PUBLIC_SUPABASE_URL`, keys) in `docs/system/admin_app/` when deployment exists.
- Vercel project wiring per PRD.

**See also:** `docs/planning/schema-gap-analysis.md` before claiming production-ready (migrations must close gaps).
