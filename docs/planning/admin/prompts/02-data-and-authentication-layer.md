# 02 — Data & authentication layer (admin)

**APD placeholder.** The single consolidated build prompt is `01-foundation-setup.md`.

For this phase in isolation, execute **only** the portions of prompt `01` that cover:

- Supabase server/client wiring
- Login + `admin_profiles` role fetch
- `middleware.ts` route protection

Then return to sequential execution of the remainder of `01` before treating later phases as complete.

**See also:** `../planning/technical-implementation.md` §2 (Auth + RBAC).
