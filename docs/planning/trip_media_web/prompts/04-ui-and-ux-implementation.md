# 04 — UI & UX implementation (Trip Media Web)

**Goal:** Polish per `ui-design-system.md`; moderation states; billing empty states.

## Deliverables

1. Sidebar layout, responsive tables, loading skeletons.
2. Moderation UX: `pending_review` / `rejected` with reviewer note from admin (read-only in partner app).
3. Trial/credits banners and subscription status chips.
4. Accessibility pass on forms (labels, errors, focus).

## Acceptance

- Design review against `ui-design-system.md` checklist.
- No raw provider errors shown to users—mapped copy.
